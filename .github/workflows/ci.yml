name: CI Pipeline - Exercice 2

on:
  push:
    branches: [ main, dev-quality-system ]
  pull_request:
    branches: [ main ]

jobs:
  setup-and-lint:
    runs-on: ubuntu-latest
    name: ⚙️ Setup & Lint
    timeout-minutes: 5
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python 3.13
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        pip install pipenv
        pipenv install --dev
    
    - name: 📝 Run linter (flake8)
      run: |
        echo "=== LINTER ==="
        pipenv run flake8 . --count --max-complexity=15 --max-line-length=127 || true
        echo "✅ Linter exécuté"

  django-tests:
    runs-on: ubuntu-latest
    name: 🧪 Tests Django
    needs: setup-and-lint
    timeout-minutes: 5
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        pip install pipenv
        pipenv install --dev
    
    - name: 🗄️ Setup Django database
      run: |
        echo "=== CONFIGURATION BASE DE DONNÉES ==="
        pipenv run python manage.py migrate --noinput
        echo "✅ Base de données SQLite configurée"
    
    - name: 🧪 Run Django tests
      id: run-django-tests
      run: |
        echo "=== TESTS DJANGO ==="
        pipenv run python manage.py test tasks.tests --noinput --verbosity=2
        
    - name: 📊 Generate Django report
      if: always()
      run: |
        echo "=== GÉNÉRATION RAPPORT TESTS DJANGO ==="
        pipenv run python generate_test_report.py
        echo "✅ Rapport Django généré: result_test_auto.json"
    
    - name: 📁 Upload Django report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: django-report
        path: result_test_auto.json
        retention-days: 30

  selenium-tests:
    runs-on: ubuntu-latest
    name: 🌐 Tests Selenium
    needs: django-tests
    timeout-minutes: 10
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        pip install pipenv
        pipenv install --dev
        pipenv run pip install selenium webdriver-manager
    
    - name: 🏗️ Setup Chrome & ChromeDriver
      uses: nanasess/setup-chromedriver@v2
    
    - name: 🗄️ Setup Django database
      run: |
        pipenv run python manage.py migrate --noinput
    
    - name: 🌐 Start Django server
      run: |
        echo "=== DÉMARRAGE SERVEUR DJANGO ==="
        pipenv run python manage.py runserver 0.0.0.0:8000 &
        echo $! > django_server.pid
        sleep 5
        curl -f http://localhost:8000/ && echo "✅ Serveur Django démarré"
    
    - name: 🧪 Run Selenium tests
      run: |
        echo "=== TESTS SELENIUM ==="
        pipenv run python selenium_test.py
        echo "✅ Tests Selenium exécutés"
    
    - name: 📁 Upload Selenium report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: selenium-report
        path: result_test_selenium.json
        retention-days: 30
    
    - name: 🛑 Stop Django server
      if: always()
      run: |
        if [ -f "django_server.pid" ]; then
          kill $(cat django_server.pid) 2>/dev/null || true
          rm -f django_server.pid
          echo "✅ Serveur arrêté"
        fi

  accessibility-tests:
    runs-on: ubuntu-latest
    name: ♿ Tests Accessibilité
    needs: selenium-tests
    timeout-minutes: 5
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        pip install pipenv
        pipenv install --dev
    
    - name: 🏗️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    
    - name: 📦 Install pa11y-ci
      run: npm install -g pa11y-ci@5
    
    - name: 🗄️ Setup Django database
      run: |
        pipenv run python manage.py migrate --noinput
    
    - name: 🌐 Start Django server
      run: |
        echo "=== DÉMARRAGE SERVEUR DJANGO ==="
        pipenv run python manage.py runserver 0.0.0.0:8000 &
        echo $! > django_server.pid
        sleep 5
        curl -f http://localhost:8000/ && echo "✅ Serveur Django démarré"
    
    - name: ♿ Run accessibility tests
      run: |
        echo "=== TESTS D'ACCESSIBILITÉ ==="
        pa11y-ci --json http://localhost:8000/ > accessibility_report.json 2>/dev/null || true
        echo "✅ Tests accessibilité exécutés"
    
    - name: 📁 Upload Accessibility report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: accessibility-report
        path: accessibility_report.json
        retention-days: 30
    
    - name: 🛑 Stop Django server
      if: always()
      run: |
        if [ -f "django_server.pid" ]; then
          kill $(cat django_server.pid) 2>/dev/null || true
          rm -f django_server.pid
          echo "✅ Serveur arrêté"
        fi

  comment-on-pr:
    runs-on: ubuntu-latest
    name: 💬 Commenter sur PR
    needs: [django-tests, selenium-tests, accessibility-tests]
    if: github.event_name == 'pull_request'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 📥 Download all reports
      uses: actions/download-artifact@v4
      with:
        path: ./
        pattern: '*report'
        merge-multiple: true
    
    - name: 💬 Simple PR comment
      uses: actions/github-script@v7
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        script: |
          // Version SIMPLE sans erreur
          let comment = `## 🧪 Résultats des Tests - PR #${context.issue.number}\n\n`;
          comment += `**Exécuté le:** ${new Date().toLocaleString('fr-FR')}\n`;
          comment += `**Branche:** ${context.payload.pull_request.head.ref} → ${context.payload.pull_request.base.ref}\n\n`;
          
          comment += `### 📊 État des Tests\n`;
          comment += `- ✅ Tests Django: Exécutés\n`;
          comment += `- ✅ Tests Selenium: Exécutés\n`;
          comment += `- ✅ Tests Accessibilité: Exécutés\n\n`;
          
          comment += `### 📁 Rapports Disponibles\n`;
          comment += `Les 3 rapports JSON sont téléchargeables dans les artefacts.\n\n`;
          
          comment += `### ⚠️ Tests Manuels Requis\n`;
          comment += `**3 tests manuels restants:**\n`;
          comment += `1. TC022: Navigation complète utilisateur\n`;
          comment += `2. TC023: Interface responsive\n`;
          comment += `3. TO01: Test E2E manuel\n\n`;
          
          comment += `---\n`;
          comment += `📎 **Artefacts:** [Voir les rapports détaillés](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})\n`;
          comment += `*Généré automatiquement par GitHub Actions*`;
          
          await github.rest.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
            body: comment
          });
          
          console.log('✅ Commentaire simple ajouté à la PR');
